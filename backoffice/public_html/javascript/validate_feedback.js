function trim(a){    return a.replace(/^\s+|\s+$/,'');}function getXMLHTTP() { //fuction to return the xml http object    var xmlhttp=false;    try{        xmlhttp=new XMLHttpRequest();    }    catch(e)	{        try{            xmlhttp= new ActiveXObject("Microsoft.XMLHTTP");        }        catch(e){            try{                xmlhttp = new ActiveXObject("Msxml2.XMLHTTP");            }            catch(e1){                xmlhttp=false;            }        }    }    return xmlhttp;}	//validate feedback..............function validate_feedback(feedback_form){    var numberRegex = /^[0-9]+/;    var emailRegex = /^[\w-\.]+@([\w-]+\.)+[\w-]{2,4}$/;    var visitors_name = feedback_form.visitors_name.value;    var company  = feedback_form.company.value;    var phone_no  = feedback_form.phone_no.value;    var time_to_call  = feedback_form.time_to_call.value;    var email  = feedback_form.email.value;    var comments  = feedback_form.comments.value;         if(visitors_name=="")    {        var msg;        msg=$("#validate_msg1").html();        inlineMsg('visitors_name',msg,4);        return false;    }    if(company == "")    {        var msg;        msg=$("#validate_msg2").html();        inlineMsg('company',msg,4);        return false;    }    if(phone_no == "")    {        var msg;        msg=$("#validate_msg3").html();        inlineMsg('phone_no',msg,4);        return false;    }    if(time_to_call == "")    {        var msg;        msg=$("#validate_msg4").html();        inlineMsg('time_to_call',msg,4);        return false;    }       if(!time_to_call.match(numberRegex))    {        var msg;        msg=$("#validate_msg5").html();        inlineMsg('time_to_call',msg,4);        return false;    }          if(email == "")    {        var msg;        msg=$("#validate_msg6").html();        inlineMsg('email',msg,4);        return false;    }    if(!email.match(emailRegex))    {        var msg;        msg=$("#validate_msg7").html();        inlineMsg('email',msg,4);        return false;    }        if(comments == "")    {        var msg;        msg=$("#validate_msg8").html();        inlineMsg('comments',msg,4);        return false;    }         return true;}$(document).ready(function(){     $("#phone_no").keypress(function (e)      {            //if the letter is not digit then display error and don't type anything            if( e.which!=8 && e.which!=0 &&  (e.which<48 || e.which>57))            {                //display error message                $("#errmsg1").html("Digits Only").show().fadeOut(1200,0);                 return false;            }	        });       $("#time_to_call").keypress(function (e)      {            //if the letter is not digit then display error and don't type anything            if( e.which!=8 && e.which!=0 && e.which!=65 && e.which!=77 && e.which!=80 && e.which!=97 && e.which!=109 && e.which!=112 && e.which!=46 && e.which!=58 &&  (e.which<48 || e.which>57))            {                //display error message                $("#errmsg2").html("Valid Time Only").show().fadeOut(1200,0);                 return false;            }	        }); });